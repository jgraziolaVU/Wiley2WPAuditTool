# A2 Hosting WordPress Manager - Clean Requirements File
# Compatible with Streamlit Cloud deployment

# Core Streamlit Framework
streamlit>=1.28.0

# HTTP Requests and API Communication
requests>=2.31.0
urllib3>=1.26.0
certifi>=2023.7.22

# PHP Data Handling for Softaculous API (optional - graceful fallback if missing)
phpserialize>=1.3

# Security and SSL Certificate Handling
pyOpenSSL>=23.2.0

# Additional Dependencies
python-dateutil>=2.8.2

# Note: xml.etree.ElementTree is built-in to Python - no need to install
# Note: All other modules (json, csv, io, os, etc.) are built-in
